# 项目开发约定

## Windows PowerShell UTF-8

在 Windows PowerShell 5.1 中读取源码、Markdown、JSON 或其他文本文件时，
假设 UTF-8 无 BOM 可能被错误识别。

- 搜索文件和文本优先使用 `rg` / `rg --files`。
- 读取可能包含中文或其他非 ASCII 内容的项目文件时，使用
  `Get-Content -Encoding UTF8 ...`。
- 不要对这些项目文件使用未指定编码的 `Get-Content`。

## 面向增长点采用声明式设计

实现“注册、默认补齐、迁移规则、处理器目录、类型映射”等天然可能继续增加条目
的功能时，不要把首个具体需求直接写死在执行流程中。

优先拆成两层：

1. 声明层：使用列表、注册表或数据对象描述每个条目的匹配条件、默认值和行为参数。
2. 执行层：使用与具体条目无关的通用遍历、校验、冲突处理和应用逻辑。

在提交实现前，使用“再增加第二个同类条目”作为抽象检查：

- 如果新增条目只需增加一段声明或注册信息，抽象通常合适。
- 如果仍需修改条件分支、复制执行流程或改动核心引擎，应继续提炼通用机制。

同时避免为了假想需求过度抽象。仅当需求表现为集合、注册表、规则序列，或用户明确
表达后续会继续补充同类项时，优先采用上述声明式结构。默认配置应只有一个事实来源，
业务页面和引擎不得各自复制同一份默认值。
